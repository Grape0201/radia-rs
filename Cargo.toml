[package]
name = "radia-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"

[[bench]]
name = "kernel_benchmark"
harness = false
[dev-dependencies]
criterion = "0.8.2"
